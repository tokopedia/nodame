var assetmanager = require('assetmanager');

function assets(app) {
    var configDir = nodame.appPath() + '/configs';
    return assetmanager.process({
            assets: require(configDir + '/assets.json'),
            debug: nodame.isDev(),
            webroot: 'assets/'
        });
}

module.exports = assets;
